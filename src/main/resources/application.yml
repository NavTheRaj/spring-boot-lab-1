spring:
  application:
    name: springlab2
  datasource:
    password: VZZCFD2CZ60A1T07$
    url: jdbc:mysql://springdb-server.mysql.database.azure.com:3306/spring-db-server??useSSL=true&requireSSL=true
    username: khjjxpapru
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    defer-datasource-initialization: true
    generate-ddl: true
  sql:
    init:
      mode: always
server:
  port: 8080
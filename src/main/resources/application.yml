spring:
  application:
    name: springlab2
  datasource:
    password: D23R7C7Q7DOP0AA8$
    url: jdbc:postgresql://localhost:5432/postgres
    username: gethebdzpy
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    defer-datasource-initialization: true
    generate-ddl: true
  sql:
    init:
      mode: always
server:
  port: 8080